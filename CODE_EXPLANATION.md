# 代码与算法说明

## 1. 数据约定

复信道张量形状为 `(P, 256, 4, 192)`，依次表示采样点、基站阵列、UE 天线和子载波。基站阵列的 256 维严格按“从外到内遍历极化 → H → V”展开。

`physical_ai/data.py` 负责读取配置和数组，并以实际数组长度作为权威样本数。训练集中 16 个全零响应通过谱特征能量检测，在训练、验证及推理参考库中统一剔除。

## 2. 地图上下文

`prepare_map.py` 与 `physical_ai/map_encoder.py` 将 PLY 点云栅格化。对每个采样位置提取局部地图块、障碍密度、高度统计以及基站—终端走廊特征。地图只用于学习条件和空间度量，不执行传统射线追踪。

## 3. 评分对齐谱特征

`prepare_features.py` 和 `physical_ai/features.py` 从复信道提取：

- PAS：沿 256 维基站阵列做正交 FFT 后的角域功率谱，按 4 个 UE 天线压缩；
- PDP：沿 192 个子载波做正交 FFT 后的时延域功率谱，按 2 个极化和 4 个 UE 天线压缩。

这些特征直接对应最终指标中权重各为 0.4 的两个余弦相似度分量。

## 4. Physical AI 谱预测

`physical_ai/model.py` 的 `MapConditionedKernel` 根据查询点位置、邻居相对几何和地图上下文预测 PAS/PDP 邻居权重。最终版本同时使用多组局部克里金专家：

- 不同邻居数、协方差尺度、nugget 与非负约束形成专家库；
- 4 个 PAS 子组和 8 个 PDP 子组分别学习专家权重；
- PAS 权重由位置和地图条件进行强正则化修正；
- 根据位置、地图条件和各克里金专家间的分歧，使用三随机种子残差注意力门控自适应修正 PAS 专家权重；PDP 使用独立单门控器；
- 训练点与查询点组成联合空间图，执行无标签传播以增强空间连续性。

相关搜索与无泄漏五折验证位于 `experiments/`。

## 5. 复信道初值生成

`predict.py` 首先寻找测试点的局部训练邻居，然后执行：

1. 按基站到采样点的距离差补偿径向载波相位；
2. 根据方向余弦差补偿阵列 H/V 导向相位；
3. 对 2 极化 × 4 UE 子组使用不同 ordinary kriging 核；
4. 从邻居复信道归一化 Gram 矩阵的主特征向量估计局部相位同步修正；
5. 对补偿后的邻居信道加权求和，得到保留复相位结构的物理初值。

## 6. 物理谱重建

预测的紧凑 PAS/PDP 特征先扩展为兼容功率目标。`physical_ai/spectral.py` 与 `predict.py` 在角域和时延域交替替换傅里叶幅度，同时保留当前相位。五折搜索表明 10 次投影比 20、40、80 次更适合不完全兼容的预测谱目标。

重建后再进行：

- 物理初值融合，避免相位和 NMSE 退化；
- 邻居几何平均能量修正；
- 2 极化 × 4 UE 分组能量比例修正；
- 由位置、点云物理条件、最近邻距离、预测总能量和极化/UE 能量比例驱动的逐样本复数尺度标定；
- 对折外统计按 `global_index` 去重，以终端远场、直达路径材料和局部地图块度量插值局部复数残差；三者验证选权为 `0.1/0.7/0.2`，并将总残差幅度限制为 `0.1`；
- 10% 强度的 UE 层级复数残差。逐 UE 复数尺度不会改变各 UE 的归一化 PAS/PDP 形状，因此只针对 NMSE 优化。

最终默认参数记录在 `Round1_Test_Channel.json`。

## 7. 评估与验证

五个固定验证折分别包含 200 个验证样本。校准器采用严格 leave-one-fold-out：训练时额外排除所有与当前验证折 `global_index` 重合的统计行。最终 v50 五折均值为 PAS `0.7648172498`、PDP `0.8519760847`、NMSE `0.6143360417`，最终综合分数 `0.7706072767`。

`verify_submission.py` 分块验证 NumPy 文件头、形状、类型、有限值、全零元素以及 PAS/PDP 探针。`tests/` 覆盖数据形状、能量兼容性、幅度投影、邻域权重和提交文件格式。

## 8. 未上传内容

为遵守数据要求并控制仓库体积，以下内容由 `.gitignore` 排除：

- 原始训练信道、训练/测试位置、PLY 地图和赛题 PDF；
- `artifacts/` 中的中间特征、模型检查点和搜索统计；
- 历史输出、备份及 ZIP 包。

仓库只保留源代码、测试、说明文档和最终输出 `Round1_Test_Channel.npy/json`。
